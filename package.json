{
	"name": "@redis-x/client",
	"version": "0.3.0-beta.8",
	"description": "Enhanced redis client built on top of \"redis\" package.",
	"publishConfig": {
		"access": "public"
	},
	"type": "module",
	"main": "dist/esm/main.js",
	"types": "dist/esm/main.d.ts",
	"exports": {
		".": {
			"import": "./dist/esm/main.js",
			"require": "./dist/main.cjs"
		}
	},
	"engines": {
		"node": ">=14.8.0"
	},
	"peerDependencies": {
		"redis": "^4.6"
	},
	"devDependencies": {
		"@kirick/eslint-config": "^0.1.30",
		"@types/bun": "^1.1.6",
		"eslint": "9.8.0",
		"oxc-parser": "0.48.1",
		"typescript": "5.7.3",
		"vitest": "1.3.1"
	},
	"scripts": {
		"build": "bun run build/main.ts && bun run build:ts && bun run build:cjs",
		"build:cjs": "bunx esbuild --bundle --platform=node --format=cjs --packages=external --outfile=dist/main.cjs dist/esm/main.js",
		"build:ts": "rm -rf dist/esm types ; tsc",
		"lint": "eslint . && tsc --skipLibCheck --noemit",
		"publish:npm": "bun run build && bun run lint && bun run test && npm publish",
		"redis:down": "docker stop test-redis",
		"redis:up": "docker ps | grep test-redis >/dev/null || docker run --rm -d -p 16379:6379 --name test-redis redis:7-alpine",
		"test": "bun run redis:up && npm run test:vitest && bun test --coverage",
		"test:vitest": "vitest run --no-file-parallelism"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/redis-x/client.git"
	},
	"keywords": [
		"redis",
		"client",
		"transaction",
		"multi"
	],
	"author": "Daniil Kirichenko (https://twitter.com/kirickme)",
	"license": "MIT",
	"bugs": {
		"url": "https://github.com/redis-x/client/issues"
	},
	"homepage": "https://github.com/redis-x/client#readme"
}
